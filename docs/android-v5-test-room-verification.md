# BX68 样机切换 V5 / IT灯塔-Test（2026-09-20）

> **2026-09-22接入已变更：** 同一设备新Wi-Fi地址为10.0.51.221，已直接连接 `https://roombeacon.thundersoft.com` 并绑定生产IT灯塔-Test，无ADB转发；生产V5功能尚关闭。当前状态见[正式接入记录](android-production-connection-2026-09-22.md)，下文为旧测试环境历史。

用户要求将 `10.0.68.146:5555` 的13.3寸BX68样机从北京-203 / V4切到最新V5 / IT灯塔-Test，并已明确正式运行不应依赖ADB转发。

用户随后确认两个网段当前本就不互通，明确授权先用ADB转发完成V5测试，正式部署时再提供互通网络。因此下方直连阻塞保留为排查记录，当前测试采用设备8768转发至开发机18766的已有SSH真实服务器通道，直接加载服务器最新静态页面和认证API，不使用旧203的V4静态副本。

**当前结果：已完成V5及IT灯塔-Test绑定，真实展示与自动心跳均HTTP200，应用重启后绑定保留。当前预约已过19:55确认截止，按钮禁用，需新预约继续测试；没有强制重置旧预约保护状态。**

## 已准备

- 只读核对现有服务器 `10.0.24.208`：静态发布 `c56365391c949a3001c93cc1e37ca9978f4e09ca`，后端工作目录为 `f3a07ea28368a7203ac218292a5901eb980fd0d2`。本次未部署服务器源码或修改服务配置。
- 指定房间ID `omm_d42ad8a9e50c5ddf9d60fe3d3bc6473b`，真实名称 `IT灯塔-Test`。检查时owner=v5、mode=auto、未全局暂停；保留既有策略和单次实例保护。
- 签发前确认该房间尚无展示及操作凭证，首次签发两种独立凭证并分别在服务器、本地忽略目录中以600权限保存。保留30天有效期，没有轮换北京203或其他房间凭证。
- 从开发机通过服务器直连分别读取展示及V5接口，均HTTP200、房间一致；这不是平板直连通过。
- Android 0.2.4-debug（versionCode 6）为调试包放行指定服务器的HTTP默认端口，正式包仍仅HTTPS。保留0.2.3的BX68低电平GRB灯控配置，未写第四路或485接口。
- 新增精确IP、其他IP、伪装域名、非默认端口、带用户信息地址及正式包禁止HTTP的回归，先失败后修复。JDK17 / SDK35 / Gradle8.9离线运行单元测试、lintDebug及assembleDebug通过，11项单元测试通过。
- 已覆盖安装新包并通过原生维护密码界面尝试将origin切为 `http://10.0.24.208`，保留维护密码、横屏及灯控开关。切换origin按界面流程清除旧网页绑定；网络不通后按用户追加授权改用下方测试通道完成绑定。

## 直连排查记录（后续已授权使用ADB测试）

实测到服务器的路由走 `wlan0`，源地址 `10.0.68.146`、网关 `10.0.68.1`；`eth0`为down。用户提到接入的485线不能据此认定有线以太网已连接。

平板到 `10.0.24.208:80` 的独立TCP/HTTP探针超时，原生页面加载超时；服务器80端口正常监听，返回路由经 `10.0.24.1`，服务器nft默认允许，443未监听。故当前不能完成真实页面绑定与V5心跳验收，需要连接可达服务器的有线网络，或解决当前网段的连通性。没有恢复ADB反向转发来绕过此阻塞。

未替用户点击确认使用、提前结束或登记允许释放；未伪造日程或心跳；未验证完整重启、断网恢复和485通信。

## 版本与回退

- 已安装APK SHA256：`1a0fc07b61435000ef11ce3f01eb50efdad0e7cd1f38e763dc9c12af59fceea8`。
- 旧APK保存在忽略目录 `.local-tools/device-v5/rollback-0.2.3.apk`，SHA256 `45753270b9f6c2cf1e90f5b33be3f23fb6c68a844f16c9ee105b3c3bf76d1fff`。
- 优先保留0.2.4；如需回到此前203临时测试配置，可在维护页恢复原origin并用已有私密203凭证重新绑定V4，这仍依赖旧测试通道，不能作为正式运行方案。不要清除应用数据或轮换其他房间凭证来回退。
- 网络接通后需补做：真实IT灯塔-Test / V5绑定、使用独立操作凭证的自动心跳、清除所有业务转发后的重新加载、物理网络接口和灯控读回核对。正式上线仍需HTTPS及签名发布验收。

## ADB测试接入完成

```bash
adb -s 10.0.68.146:5555 reverse tcp:8768 tcp:18766
```

开发机18766是已有SSH到真实服务器80端口的通道；设备origin为 `http://127.0.0.1:8768`。通过原生维护界面切换后，在实际网页分别绑定该测试房间的展示和操作凭证，持久保存 `argus_room_version=v5`。应用使用服务器发布 `c56365391c949a3001c93cc1e37ca9978f4e09ca`，没有复制旧V4页面或伪造接口。

- 真实展示HTTP200、房间ID及名称正确；V5主动心跳HTTP200，服务端返回对应测试房间。
- 强制停止并重启应用后V5、房间和操作绑定保留。新页面会话初次心跳409属于原有45秒会话保护，等旧会话自然过期后恢复200；没有清理服务器会话或强行重置实例。
- 实际1280×720 / DPR1.5，整页无溢出；状态busy，IO1/2/3=101、IO4=0，符合BX68红灯配置。
- 最终当前实例为 `blocked/missed_window`，确认截止为北京时间19:55，`can_confirm=false`、按钮禁用；全局paused仍为false。「自动释放已暂停」指当前受保护实例，不等于本次关闭了全局释放。
- 后续测试需要新的有效预约。未点击确认使用或提前结束，未改动策略、白名单或登记新实例允许释放。
- 当前仍依赖ADB与SSH测试通道；设备重启会清除反向转发，需要重建。正式以太网、HTTPS和断电独立恢复另行验证。
