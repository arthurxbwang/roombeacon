# 2026-09-21 夜间预约未释放排查

## 实际运行与结论

08:45–08:46 只读核查既有 Control：仍运行 `85a3cf2e9afbdc0183e2f9c30ab1156011aa6a09`，进程自前晚23:20启动后未重启。V5写入开关打开，全局未暂停，服务端白名单仅含IT灯塔-Test。无非重复登记审计，无释放认领或结果审计；不是飞书返回失败。

截至08:46，今日已产生22条实例记录：

| 预约开始时间 | 数量 | 结果 |
|---|---:|---|
| 00:00 | 1 | 23:50:01成功提前登记，00:01:32因监控中断受保护 |
| 00:30 | 1 | 到00:30才登记，missed_window |
| 01:00–08:30 | 19 | 均提前约10分钟登记，到签到截止时 release_not_enabled，verified=false |
| 08:45 | 1 | 提前登记，核查时pending，verified=false |

主要原因是保留了逐实例“已核对非重复且允许释放”的试点门槛。这批预约没有完成登记，因此单纯开启房间自动模式不会发送释放。前晚新建多场预约不等于这些实例已被自动登记；需要明确这一产品/测试前提，不能把无请求描述为释放接口已验证。

01:00之后的实例均在上一场仍占用时提前进入pending，说明连续会议提前监控修复已经生效；这仍不能证明飞书真实释放结果。

## 零点独立缺陷

只读Nginx日志：23:59:55至00:01:45有110秒心跳间隔；相邻心跳均HTTP200。00:00:05至00:01:25出现9条读取失败。工作者00:00:00至00:01:30连续记录46次AppError。00:00实例最后更新监控时间为23:59:58。

源码 `refresh_batch` 请求前一天到后天零点的忙闲数据，却将valid_until强行截断在次日零点。零点前已覆盖下一天的缓存因此提前失效，直到新采集到达；V5监控间断触发保护。00:30未成功提前登记；其与页面不确定状态的关联依据现有状态机推断，历史日志没有心跳请求体，不能宣称已直接观测该状态值。

修复保持原有300/360秒新鲜度，最大有效期仍不超过实际查询终点，不再无条件截断在中间的零点。171项后端测试及Ruff通过；3项新测试先复现失败，覆盖跨日仍可读取本场、到期拒绝、上游失败不延长缓存；原有过期/故障保护不放宽。修复仅源码和隔离验证，尚未部署，以免重启中断正在监控的新实例。

## 当前操作范围

本轮只读调查线上数据，未签到、未调用释放、未登记新实例、未修改策略或重置blocked记录。已询问用户这批是否全为不重复，收到事实确认后才能登记尚未错过窗口的测试实例；不能把未回复解释为确认。已过期预约不补释放。
