# 已签到预约延后半小时观察（2026-09-21）

## 测试范围

用户先签到，再将同一会议从17:30–17:45延后到18:00–18:15，要求观察不再次签到是否会释放。本轮仅观察现有规则，不替用户签到、不登记释放、不修改Redis状态或策略。

## 初始证据

- 原始时间和改期后记录的会议UID哈希相同，原始时间字段均为0；开始及结束改变产生不同RoomBeacon实例身份。
- 原时间记录保持confirmed，确认时间17:35:59；新时间记录17:50:01提前监控，pending/monitoring、verified=false，截止18:10。
- 实机17:53显示18:00–18:15、签到截止18:10，提示自动释放待管理员登记本次预约。平板心跳ready，缓存新鲜。
- 因新记录未登记释放资格，代码预计截止后blocked/release_not_enabled；不以此证明“改期继承原签到”。最终以实际审计和飞书读回为准。

## 最终结果

18:10:01.357新时间记录转为blocked/release_not_enabled，未进入waiting、checking或releasing，无claim或释放结果；原时间confirmed保持。18:10:45独立飞书读取仍包含18:00–18:15预约；18:10:49心跳ready，日程尚有效。实机显示“自动释放已暂停／本次预约受保护，不会自动释放”；初始画面已提示待管理员登记。

结论：本次未签到不释放，直接原因是新时间未登记释放资格；不是继承了旧签到，也不是飞书释放失败。仍不能据此判定“获得资格后的改期会议是否应该沿用签到”，那是下一步业务规则与实现问题。

## 验收边界

这是B04中“改时间后不继承旧签到或释放资格”的一部分观察，不是B01“已获得释放资格的改期例外自动释放”测试，也没有验证自动登记功能。当前身份包含room_id、uid、original_time、start、end，修改任一时间会产生新身份。

18:12:04只读监控自然结束，改期后记录仍为blocked/release_not_enabled，旧签到仍confirmed。18:11采集已刷新，新鲜快照与心跳保持。监控为有18:12截止的只读脚本。证据保存在忽略目录`.local-tools/v5/reschedule-signed-*`；没有向飞书写入或伪造心跳。
