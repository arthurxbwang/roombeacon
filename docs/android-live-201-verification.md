# 北京-201 真实接口联调（2026-09-20）

## 授权与目标

用户授权调用已有门牌服务器，将 `10.0.69.167` 样机绑定北京-201；并在确认该房间已有凭证后，明确允许重新签发、使旧绑定失效。

目标唯一标识：`omm_4e3bf41620b5dea1628054d692591585`。位置：ThunderSoft / 中国大陆China / 北京 / 中国北京_创达大厦 / 2F；名称 201，容量 14 人。

## 实际接入

- 已有服务器：`10.0.24.208`，`room-display` 服务 active，部署 SHA `c34a5614bdb5e86305e40deb0d5c36807ccd0b03`。
- 已有入口只有 HTTP 80。服务器经 SSH 隧道转到开发机 `127.0.0.1:18766`。
- 开发机 `127.0.0.1:8766` 提供已验证的 WebView 106 兼容 H5，并仅转发北京-201 的设备读取接口；实际凭证仍由已有服务器校验。不存在模拟日程或主控凭证替代。
- 样机经 ADB reverse 访问 `http://127.0.0.1:8766/?version=v3`；正式 APK 的 HTTPS 策略未放宽。该地址是临时测试 origin，不是正式服务器部署地址。
- 使用既有 `issue_device` 签发函数，仅轮换北京-201 的设备凭证；凭证未写入文档、日志或 Git。没有部署或修改已有服务器源码。

## 验证结果

- 绑定表单提交后，真实设备接口返回 HTTP 200，房间 ID 与北京-201 一致。
- 后续 15 秒轮询再次返回 HTTP 200。
- 强制停止并重新启动 APK 后，自动恢复北京-201 绑定与正常同步；下一次真实接口轮询仍返回 HTTP 200。
- 首次核验缓存有效，城市为北京；初次绑定时没有配置该房间的官方签到二维码，后续配置结果见下节。没有伪造二维码或签到状态。
- 样机已移除模拟绑定与模拟标识，进入真实会议室页面。

本记录不保存会议主题、组织者、日程响应体或真实页面截图。自动审批拒绝历史令牌搜寻后，采用用户明确批准的重新签发流程；另取消真实页面截图，改用绑定布尔条件与 HTTP 状态核验。

## 官方签到码配置与自动更新（2026-09-20）

- 用户另行授权配置北京-201 官方签到链接。仅修改 `10.0.24.208` 的 `/etc/argus-room-display/service.env` 中 `ROOM_DISPLAY_CHECKIN_URLS` 对应房间项；保留原有其他房间配置，文件权限 600。源码 SHA 保持上述版本。
- 修改前在同目录创建 `service.env.before-201-checkin-时间戳` 私有备份，重启 `room-display` 后确认 active、有效环境中的目标链接与用户输入完全一致、其他房间映射未变。回退可恢复本次备份并重启服务；不要覆盖后来新增的配置。
- 服务端二维码生成成功。测试中发现上次临时转发进程已停止，恢复 SSH 与本机代理后，样机在原页面自动显示签到二维码；未重启 APK、未手动刷新页面、未重新绑定。
- 实机核验：北京-201 真实接口 HTTP 200、页面同步有效、二维码图片加载成功且与接口返回一致。没有导出真实截图、会议内容、二维码图像或链接。
- 新增隔离浏览器测试验证未配置→新增→更换→移除均随轮询生效，页面没有导航且保留原绑定；定向测试 1 项通过。更换和移除未在真实房间上执行，没有用模拟二维码替换用户链接。
- 当前配置来自启动环境，更换链接仍需更新服务端配置并重启服务；终端下一次成功轮询（正常间隔 15 秒）获取，无需升级 APK。手机飞书实际扫码及签到业务结果尚未验收。

## 当前运行边界

### V4 样机验收（2026-09-20）

V4 继承当前 V3 功能和协议 1 灯控状态；移除四周状态边框，横屏后续会议卡片上下内边距缩为 12px、间距 12px，时间/标题/组织者字号分别为 28/26/18px，标题最多两行并保留完整 title 提示。其他尺寸仍保留可滚动访问。

V4 加入主控选择；根入口没有历史选择时默认 V4，旧版本 URL 与浏览器版本键继续兼容。Android 0.2.1（versionCode 3）不再写死 V3，打开根入口；样机保存 V4 选择并保留原房间和灯控配置。后续 UI 版本更新不必再改 APK 入口。

前端全量 49 项中 48 项通过，1 项旧版测试因新加初始化在刷新时覆盖版本选择而失败，修正为仅首次初始化后定向复跑通过；V4 两种入口的布局回归通过，覆盖两行长标题、签到、灯控状态、边框移除及上滑稳定。Android 7 项单元测试、lint、assembleDebug 通过。

恢复丢失的 ADB reverse 后，实机检查得到两张会议卡片、门牌及后续会议容器溢出均为 0、边框为 none、签到二维码加载正常。上滑后页面偏移为 0；业务 busy 时 GPIO 为 100、IO4 保持 1。ADB 截图仅存在本地忽略目录，未导出真实日程响应或将截图入库。

安装的调试 APK SHA256：`51a3e8c4571f846fbd69ea3fd57ccfa6a949f9a15ef933ebedc23e73c18c2cdf`。本次更新作用于临时样机 H5 入口与 APK，未部署旧服务器源码，未推送 Git。

### 底部上滑布局修复（2026-09-20）

用户明确授权 ADB 截图排查。实测 WebView 视口始终为 1280×800，门牌容器高 800、内容滚动高度 833、上滑偏移 33；窗口检查未发现持续导航栏占位。根因为 V3 内容区 `min-height:480px` 导致外层溢出，容器滚动时状态边框也偏移。横屏且宽高均大于 700 时允许内容区收缩，内部面板继续可滚动；小屏和竖屏保留原有滚动布局。

回归测试先复现 33 像素溢出，再验证修复。H5 构建成功；全量测试 40 项通过，另 1 项遇浏览器资源不足，改用有空间的临时目录单独复跑通过。样机加载修复后的 H5，分别从底部三个位置上滑，均保持视口高 800、整页溢出 0、滚动偏移 0、二维码加载正常；ADB 修复后截图确认边框与页头页脚完整。未升级 APK、未修改 Android 系统设置或部署旧服务器源码。真实截图仅留在本地忽略目录，不入 Git。

样机仍依赖开发机转发进程、SSH 会话和 ADB 隧道。它验证了现有真实服务与 APK 的兼容性，不等于独立服务器部署、实际断电恢复或长期稳定性验收。

现有凭证语义保持 30 天有效、重新签发同房间会使旧凭证失效。后续独立 HTTPS 服务就绪后，从原生维护入口更换服务器并重新绑定。不要把设备指回模拟 origin `8765`，也不要对已绑定真实服务的页面运行模拟数据脚本。
