# V5 逐项验收清单

更新：2026-09-21。状态依据已保存的真实验收记录，不等同于刚刚重新检查线上。范围：仅IT灯塔-Test；其余342间保持V4官方方案。

## 当前结论

最近16:15场于16:26:11释放成功，独立飞书查询10→9只少当次，明天16:00改期例外、后天同系列和其余预约保留。本场未发现误释放或重复执行。历史发现的问题与尚未开发的能力仍需区分，不能称V5已经完整验收。

## 已实测通过

- [x] A01 普通预约平板签到后保留；服务器有确认审计。
- [x] A02 普通预约不签到自动释放；独立飞书读回、缓存和平板一致。
- [x] A03 连续预约分别提前监控；前场签到或释放不污染下一场。
- [x] A04 每日重复普通实例只释放当次，所查窗口内后续实例保留。
- [x] A05 普通重复实例释放后，同系列后续改期例外仍保留（16:26增补）。
- [x] A06 仅测试房开放V5；其余342间官方方案、凭证与官方二维码配置保持。
- [x] A07 提前结束入口关闭，旧操作路径不能提前结束。

## 逐项待验收

| 编号 | 状态 | 场景与通过标准 | 配合方式 |
|---|---|---|---|
| B01 | 待实测 | 改期实例本身不签到，只释放该实例；其他日期保留 | 用户选择某一次并仅修改本次；提交后以实际开始时间及原始实例标识锁定 |
| B02 | 待实测 | 同一重复系列一场签到、另一场不签到；分别保留和释放，不继承签到 | 用户安排同系列两次不重叠的实例；测试时逐实例登记 |
| B03 | 待实测 | 提前监控建立后取消预约；不再发送该预约释放，后续预约正常 | 用户取消锁定的测试预约，我核对监控及审计 |
| B04 | 部分通过 | 提前监控建立后改时间或移除测试会议室；旧身份不再写入，新身份不继承签到及释放资格 | 用户仅修改指定实例；新时间留足提前监控时间 |
| B05 | 待实测 | 签到截止后一分钟缓冲内补签到，预约保留；连续点击仅产生一次有效确认 | 用户建普通预约，点击及审计由我操作验证 |
| B06 | 待实测 | 平板在释放前断网超过心跳阈值、关闭应用或重启；本场不释放，恢复后不补删，后续新场恢复正常 | 用户预留两场测试预约，我操作专用样机；三个故障子项分别留证 |
| B07 | 部分通过 | 服务重启时未确认预约进入保护已有真实记录；补测已签到记录保留、恢复后新场正常 | 用户预留预约，我安排受控重启；避免与其他验收混测 |
| B08 | 部分通过 | 忙闲API短暂504重试已有真实成功；补测持续失败或缓存过期不误报空闲、不释放；恢复不补删受保护场次 | 我在隔离环境注入故障，明确区分隔离验证和线上观察，不影响其他房间 |
| B09 | 待场景验收 | 释放返回明确失败、响应丢失、成功后读回失败；不冒充成功、不重复发送 | 我用隔离故障测试验证；真实释放后自然读回另有已通过记录 |
| B10 | 待实测 | 跨午夜会议、临近开始才同步的预约、重复系列最后一次；不漏显示、不错误补释放 | 用户按轮次预约；无法确认实例范围时须保护并明确提示 |
| B11 | 待实测 | 暂停释放、取消房间释放资格在尚未发送时生效；恢复不补发受保护预约 | 我操作测试房；共享服务器全局变更另安排，不与其他测试混测 |
| B12 | 待观察 | 24–48小时连续运行，覆盖跨日及多场预约；核对未释放原因、保活、504与数据新鲜度 | 用户准备明确允许释放的测试预约，平板保持在线；先完成逐实例登记或自动登记迭代 |

## 功能缺口，须开发后验收

- [ ] C01 取消目前试点的逐实例人工登记依赖：在指定房间自动识别并安全登记新预约，支持实例取消／改期失效；未知实例明确保护。当前房间开关不等于未来所有预约自动释放，重复登记也尚无完整Control入口。
- [ ] C02 周／月重复及短查询窗口内只有一个普通实例的识别：当前证据不足会拒绝重复登记，须完善识别后再进行真实单次释放测试。
- [ ] C03 完善504诊断：记录脱敏追踪号及准确耗时；现有限次重试已生效，但偶尔仍耗尽，根因尚未闭环。

## 执行方式

先做B01，再做B02–B05；随后安排故障保护、自动登记与周/月重复，最后长稳。继续使用IT灯塔-Test，不需要新会议室。普通测试建议每场30分钟、开始至少留20分钟且不重叠；临近开始及跨日用例单独安排。

每项通过须记录目标、实际操作、服务端状态／审计、飞书独立读回及实机表现。故障场景标明真实或隔离验证，不用mock代替线上结果；发现保护状态不清除重置来补造成功。

依据：[重复实例验收](v5-recurring-release-verification.md)、[连续三场验收](v5-retest-acceptance.md)、[504排查](feishu-504-investigation.md)。


## 17:36现场故障增补

B01保持待验收：本次被17:31飞书504重试耗尽及缓存过期打断，17:35恢复后已签到，未进行自动释放。故障期间请求仍到达服务器，状态未知保护生效；此证据补充B08，但不代替其他故障场景。详见[现场排查](v5-b01-connection-investigation.md)。


## 已签到后改期观察（18:10）

B04部分通过：同一会议由17:30延后到18:00，旧时间签到保留，新时间重新建立监控且不继承签到或释放资格；18:10:01因release_not_enabled保留，独立飞书读回仍占用。没有登记资格来改变用户手动测试条件。尚未覆盖监控中移除会议室、重新登记后释放等子场景；详见[观察记录](v5-signed-reschedule-observation.md)。


2026-09-21 开发增补：B04/C01所需日历自动核验和测试预约工具已实现并通过隔离回归；未部署，日历读取权限不足，仍不能将真实验收标为通过。详见[本轮修复](v5-reschedule-autoverify.md)。
