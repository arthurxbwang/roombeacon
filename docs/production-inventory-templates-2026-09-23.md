# 设备台账与模板编辑发布验收（2026-09-23）

## 授权与版本

用户要求先在服务器部署并验证可用性，确认正常后推送 GitHub。本次按该顺序，将已固定的本地 Git 提交通过 bundle 导入生产仓库，再由服务器按准确 SHA 导出、构建和部署；未复制未提交业务源码。

| 项目 | 记录 |
|---|---|
| 需求 | [#25](https://github.com/arthurxbwang/roombeacon/issues/25) |
| 目标 | `https://roombeacon.thundersoft.com`，SSH 8081，`tsm-eed-ts-bj`，`/data/roombeacon` |
| 新前后端 SHA | `4b65ecceb53e82333df54038d1290bf4da42dc57` |
| 旧前后端 SHA | `808710d5347353c241b438eafbe9d5ccd8979db9` |
| 发布备份 | `/data/roombeacon/backups/v6-20260923T131658Z/` |
| 验收资料 | `/data/roombeacon/backups/inventory-4b65ecc/` |
| Python 环境 | 保留 `/data/roombeacon/venvs/3b0bf39cdfdb92e421882444615f8ab7bcb76c8c` |
| APK | 保持已安装 0.6.2-debug，本次没有安装或更新 |

后续文档提交不代表再次部署。源码分支为 `codex/device-inventory-templates`，接续尚待合入 main 的型号模板分支；本次仅推送该功能分支并提交审阅，不直接推送 main。

## 真实可用性验收

北京时间约 21:17 完成服务切换；发布脚本先备份 SQLite、环境配置、Nginx 及 current 链接，再运行前端构建、Ruff 0.16.7 和后端 300 项测试，全部通过、无跳过。Nginx 配置检查、本机后端／入口及正式 HTTPS 就绪检查通过。

正式 HTTPS 上使用短期主控管理员会话进行浏览器验收，凭证仅保存在私有文件，不写入仓库或日志：

- 管理页成功读取 343 间真实会议室和 1 台设备，地区及名称搜索正常。
- BX68 配置窗口可逐级选择真实目录的 5 层位置，最后缩小到 3 间候选会议室；能够选择当前绑定房间，未提交设备换房或配置下发。台账中没有独立屏幕方向编辑控件。
- 会议室「业务方案」成功读取真实规则，仅查看，未保存规则、签到、登记资格或释放预约。
- 创建一份明确命名的临时通用屏幕模板，关闭灯控，设置竖屏；API 返回 200。再编辑为英文，版本增至 2，API 返回 200；刷新页面仍保持已保存内容。
- 用旧版本 1 再次编辑返回 409，正确拒绝覆盖。没有向设备发布临时模板。
- 浏览器脚本错误 0、HTTP 5xx 0；验收结束退出会话返回 200。
- 临时模板按本次返回的精确 ID、名称和配置核对后清理，仅移除该条模板及其版本记录，保留创建、编辑和清理审计。原有模板摘要恢复为部署前一致。

该会话验证管理登录后的实际可用性；没有让用户重新执行飞书扫码，因此不将其称作本轮飞书扫码验收。匿名管理目录及门牌数据仍返回 401，飞书登录入口配置有效。

## 采集、设备及数据完整性

服务重启后 21:17:22—21:18:32 完成第一轮真实采集：343 间、18 批次、0 失败；最终目录及有效快照均为 343。

BX68 在部署前后均在线，APK 0.6.2-debug、配置回执 5/5、错误为空；最终检查距最近心跳约 7 秒。本轮没有使用 ADB、改变设备身份、更新 APK 或修改设备配置。

部署前后对账号、首位管理员消费记录、原有模板、设备身份／绑定、设备配置及版本、旧展示凭证、V5 规则、暂停开关和写入范围做摘要比较，全部一致。释放范围仍为 1 间，检查时在途监控记录 0；未扩大其他 342 间写入权限。

浏览器截图与原始验收回执保存在开发机 `.local-tools/inventory-release/`，不提交含资产、会话或真实目录的原始文件。这些真实结果与前轮隔离测试记录分别保留。

## 回退

发布脚本已配置切换失败自动回退。本轮未触发回退。

如需回退到发布前版本，先核对当时在途任务，再使用验收目录中的 `rollback.sh`，参数为 `/data/roombeacon/backups/v6-20260923T131658Z`：恢复该备份的 `backend.before`、`nginx.before` 和服务覆盖项，重启并执行同样的 HTTPS、采集与设备检查。

本轮新增的 `template_versions` 表可保留，旧版忽略该表；原 `templates` 表结构和设备配置协议保持不变。**不要恢复旧 SQLite 整库、清空 Redis、重置管理员初始化或覆盖预约保护记录。**若回退期间修改模板，再次升级前核对模板及版本元数据，并关闭旧浏览器编辑页。Python 环境与 APK 均无需回退。
