# 生产服务器交付检查（2026-09-22）

## 当前处理结果（用户确认后）

以下结果覆盖首次检查中的对应建议：

| 项目 | 当前结论 |
|---|---|
| 异常自动恢复 | 已修复。Caddy/Nginx采用失败后5秒重启、每分钟最多5次；Caddy恢复持久化动态配置。隔离崩溃回归通过，生产未中断。见[修复与回退](production-entry-recovery-2026-09-22.md)。 |
| 备份 | 用户选择VM层备份，已记录并接受；不再要求另建应用层备份。 |
| 指标采集 | 用户后续单独配置，本轮延期处理。 |
| 证书续期 | 用户确认由自动化负责，本轮不新增续期机制。 |
| 组织者姓名 | 用户调整权限后，两位不同用户的通讯录查询均200/code0并返回正确姓名；原失败对象从41050变为41012，最新忙闲仍返回该无效ID。当前无证据需继续加权限，该对象待核对账号与上游身份数据。见[复测记录](feishu-permissions.md)。 |


## 首次检查范围（历史）

检查对象为 `roombeacon.thundersoft.com`（10.0.53.174），SSH8081，部署目录 `/data/roombeacon`。本轮检查约在北京时间14:12–14:23完成，仅执行只读检查、少量真实读取API和无凭证访问测试；未重启服务、修改生产配置、签发设备凭证或操作会议预约。

网站、HTTPS、主控鉴权与真实采集已正常运行。正式交付前建议收尾：备份恢复、业务告警、入口服务自动恢复、证书续期验证，以及飞书通讯录数据范围。终端接入与签到是否作为本次交付内容，需按实际交付范围验收。不能把“主机与网站已经运行”当成这些项目均已验证。

## 已通过的检查

- 实际代码为 `3b0bf39cdfdb92e421882444615f8ab7bcb76c8c`，服务器发布目录Git状态干净；当前应用使用独立非root账户运行，启用 `ProtectSystem=strict`、`ProtectHome` 和 `NoNewPrivileges`。
- 生产域名解析到10.0.53.174。客户端通过正常TLS验证访问HTTPS首页和Control均200，HTTP跳转HTTPS；协商TLS1.3。
- 未认证主控、门牌及V5查询均401；正确主控凭证可以读取343间目录及预览，主控凭证不能替代设备凭证。
- 343间房间缓存全部在有效期内，快照共1010条预约。14:19:20完成的一轮18批次、343间、失败0；此前抽查连续5轮也全部完成。
- 应用、Redis、Nginx、Caddy和ACBridge运行中并设置开机启动；无failed systemd单元。应用与Redis未发生自动重启。
- Redis仅监听127.0.0.1:6380，AOF开启，RDB/AOF最近写入状态正常，内存约1.32MB，最大2GB，`noeviction`，无淘汰或连接拒绝。
- 环境文件权限600；ACBridge缓存的私钥和组合PEM权限600。主控主凭证64字符，无第二组短密码。
- `/data`剩余约92GB，系统盘剩余约13GB，15GB内存中约14GB可用；负载很低，NTP同步正常。当前未发现资源瓶颈。

## 首次检查发现（历史，处理结果以上方为准）

| 优先级 | 证据 | 影响与完成标准 |
|---|---|---|
| 高：业务备份和恢复 | 本机未发现RoomBeacon业务备份定时任务；`backups`内为安装日志、初次部署记录与Nginx旧配置，未见Redis、私有配置和证书的独立备份。 | 明确备份目的地、保留周期、负责人、允许丢失的数据时间和恢复时限；覆盖Redis、私有环境、证书及入口配置，完成隔离恢复演练。若已有虚拟化平台快照或统一备份，需要提供其覆盖范围和恢复证据，本轮无法从主机证明或否认。 |
| 高：业务监控与通知 | 有node_exporter；本机未发现HTTPS/API/缓存新鲜度告警任务，外部监控平台规则未提供。 | 确认HTTPS可用性、认证后的缓存新鲜度、连续采集失败、磁盘/AOF写入及证书到期都有接收人和可验证的通知；至少做一次告警触发及恢复验证。进程存活不足以证明显示数据仍有效。 |
| 高：入口异常恢复 | `caddy.service`、`nginx.service`均为`Restart=no`；应用和Redis为`on-failure`，ACBridge为`always`。 | 为入口服务制定异常退出自动恢复策略，例如经验证的`Restart=on-failure`与合理退避；安排维护窗口验证Caddy/Nginx、Redis和应用恢复，以及整机重启后HTTPS、证书和完整采集恢复。本轮未为检查而重启生产。 |
| 交付前确认：证书续期 | 当前通配符证书到期时间为2026-10-14 23:59:59 UTC，即北京时间10月15日07:59:59，距检查约23天。Caddy关闭自动证书申请，ACBridge已收到证书并缓存，配置每30秒同步。 | 确认AllinSSL侧的续期与推送任务、负责人和到期告警，并验证新证书推送到该机器后实际生效。ACBridge同步已有证书不等于已经验证上游会续期。私钥权限和本地缓存本次通过。 |
| 实际业务问题：姓名补全权限 | 约68条`room_organizer_lookup_failed`；从实际失败日志定位一个用户，只读复查返回HTTP400、飞书`41050`。快照1010条预约中84条没有姓名。 | `41050`为通讯录数据范围无该用户权限，应将需要显示姓名的组织者纳入新应用的通讯录权限范围，再复查实际失败请求。不能认定全部84条缺失均由同一原因造成；隐私、人员类型和其他范围限制仍应保留。 |

Redis的持久化文件与独立备份用途不同，备份和恢复需单独安排，见[Redis持久化与备份说明](https://redis.io/docs/latest/operate/oss_and_stack/management/persistence/)。异常恢复策略依据[systemd服务说明](https://github.com/systemd/systemd/blob/main/man/systemd.service.xml)。Caddy手工加载证书或关闭自动证书管理后，不能假定其仍自动续期，见[Caddy Automatic HTTPS](https://caddyserver.com/docs/automatic-https)。飞书41050含义已按官方单用户接口的错误码映射核对，见[获取单个用户信息](https://open.feishu.cn/document/server-docs/contact-v3/user/get)。

## 网络与账户收口

- 当前管理网络可达80、443、8081、9443；22、8080、8088、9100连接超时。不能据本机全接口监听断言这些端口对互联网开放，也不能把当前网段的超时推广到所有网段。
- 本机UFW未启用，nftables无规则，iptables/legacy策略ACCEPT，访问范围依赖网络侧控制。应把允许来源形成交付记录：80/443面向使用网段，8081面向管理网段，9443面向证书推送来源，9100仅监控来源。
- Nginx8080监听0.0.0.0，但唯一已确认用途是Caddy到127.0.0.1:8080的代理。可在确认没有其他直接客户端后改为回环，减少绕过HTTPS的入口。
- SSH同时监听22和8081。root已禁止密码登录，但全局`PasswordAuthentication=yes`，存在启用密码的`devops`账户。交付前确认其实际登录需求；若统一采用密钥，收紧密码登录并验证备用管理通道后再移除不需要的22端口。
- HTTPS成功页面未返回HSTS、`X-Content-Type-Options`和`Referrer-Policy`，而404响应有后两者。建议统一入口安全响应头；HSTS在确认HTTPS长期稳定后配置，不直接对整个公司域名设置`includeSubDomains`。

## 终端和功能交付边界

- 新Redis内设备凭证数为0，尚不能以此证明正式门牌已接入新服务器。
- 官方签到二维码配置数为0；V5、V5飞书写入均关闭，白名单和自动核验日历映射为空。若交付范围仅为展示，这些是尚未启用的功能；若包含签到/释放，需要另行完成配置和真实验收，不能直接打开全局写入。
- 当前设备展示凭证默认30天到期，需明确签发、续期或换绑流程，并避免成批同时到期影响门牌。本轮未创建任何新凭证。
- 需要用真实终端验证HTTPS证书链、绑定、正常展示、凭证撤销和失联保护；浏览器及API检查不能替代实机交付。

## 上游稳定性与本轮限制

服务日志有2次飞书忙闲504重试，最近完整采集仍成功。本轮额外、有界的忙闲只读抽查连续3次返回HTTP504、code2200，未取得该抽查响应中的用户信息。随后直接使用既有失败请求日志中的用户身份只读复查通讯录，得到41050；没有把504误判为通讯录权限错误。

暂未证明504的飞书内部根因；本轮没有调整重试策略、采集周期或失效边界。应把连续失败和缓存过期纳入告警，并安排实际网络下的持续观察。当前有效快照不保证未来每轮均成功。

本轮没有执行业务恢复演练、整机重启、证书轮换、真实终端迁移，也无法从服务器本机确认公司统一监控、备份和网络ACL的全部外部配置。
