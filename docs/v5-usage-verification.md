# V5 确认使用：验证与运维

> 2026-09-20 Control 签到修复：原预览只读，导致专用房间无法直接测试签到。V5 预览新增「进入签到测试」，仅服务器白名单内 V5 房间可用；主控认证的 POST `/api/room-control/usage/{room_id}/confirm` 校验预约、规则、时效与原子状态后记录确认。不会签发设备凭证、上报保活、登记允许释放或执行提前结束；平板完整释放测试仍按原流程进行。

> 2026-09-20 18:54 最新生效状态：现有服务器运行 `fa09d72453da71d4186fd0f6737f1b4ed5364a4f`，仅「IT灯塔-Test」开启 V5 自动释放写入，服务器白名单仅含该房间 ID，全局暂停已解除。已逐一核对其他 342 间为官方方案且无 V5 写入资格；下方写入关闭／观察模式为前次部署记录。真实释放尚未验收。

> 2026-09-20 部署更新：用户要求的现有 [Control](http://10.0.24.208/control) 已部署 `4ba9d38`，支持 V1–V5 预览及 V5 规则；仅专用房间观察模式，飞书写入关闭，实际释放未验收。路径、版本和回退见 [部署记录](v5-control-deployment.md)。下文开发阶段的“未部署”描述不代表当前 Control 状态。

> 已按 [V4／V5 分离与保护释放计划](../plan/v5-protected-release.md)完成本地保护迭代。GET 不再保活，V5 全模式不展示官方二维码；待释放补确认、主动健康上报和发送前页面核验已实施。真实专用房间读接口通过，预约创建受应用权限阻塞，释放和实机仍未验收，见 [专用房间记录](v5-test-room-verification.md)。

更新：2026-09-20。当前为本地实现、默认关闭；未发布、未部署旧服务器、未修改真实会议或样机配置。版本号 V5 指本次 Web 功能迭代，不改变 APK 0.2.1 或仓库软件发布号。

## 已实现

- `/?version=v5`：确认使用、确认截止时间、观察状态、提前结束二次确认、独立操作凭证绑定。保留预约、时间轴及侧灯状态协议。
- 中控「V5 规则」：关闭／观察／自动、提前确认及宽限分钟数、原生策略与接口核验标记、单次非重复预约登记、全局暂停及审计。
- 服务器持久确认、原子状态转换、单工作者租约、设备心跳、单次飞书写入与结果复核。
- 旧默认仍为 V4；V5 主控预览不可执行确认或释放，也不修改既有版本偏好。
- 旧服务器没有 V5 接口时显示不支持提示，正常预约展示继续工作。
- 服务器逐房间保存 `owner=official/v5`；官方方案只能配 `mode=off`。旧策略没有 owner 时按官方关闭处理，不能自动继承写入资格。V5 房间的展示响应附带 `usage_owner=v5` 并移除官方二维码，V4 页面提示切换入口。
- 健康协议 2：页面每 10 秒主动 POST，包含页面会话、目标实例、策略版本和操作状态；GET 只读。会话 ID 保存在 sessionStorage，刷新沿用；新页面产生新会话，旧会话 45 秒有效期内拒绝抢占，切换会话的当前实例受保护。
- 确认前先上报提交状态；提交失败或日程查询失败保留本次未核实标记，跨刷新继续上报异常。服务器收到异常后将实例置为 blocked，普通状态恢复不会恢复本次自动释放；确认本身成功保存后可显示已确认。
- 正常截止后进入 waiting，默认再等待 60 秒（可设 30–300 秒），允许补确认。之后进入 checking，当前页面需在 15 秒内回复绑定实例和任期的一次核验标识。核验超时、会话不匹配、上游或缓存读取拖过期限均不发起释放。
- 提前结束保留二次确认，同样经过页面核验；V5 确认不修改飞书原生签到状态。

## 隔离验证

本轮前端通过单独复制的源码、依赖、构建输出及预览端口验证，未覆盖 `frontend/dist`、原 `node_modules` 或设备使用的转发入口。隔离目录为仓库忽略目录 `.local-tools/v5/validation/frontend`，预览端口 4185。原页面发布测试硬编码 4178，仅在隔离测试副本同步替换端口，不修改其业务逻辑。

后端用 mock 替代飞书，V5 状态测试单独启动 Redis，仅监听临时 Unix socket，测试结束销毁。真实 API 权限、通知、会议占用变化与 Android 实机操作仍待专用预约验收。

首轮结果：后端 111 项通过，浏览器分批覆盖 57 项。保护迭代新增查询与保活分离、会话隔离、待释放补确认、核验过期、操作异常保持和旧策略回退用例；本轮结果在下方补充。Android 源码未改动，本轮未重装 APK 或宣称实机通过。

保护迭代最终结果：Ruff 0.16.7 通过，后端全量 **129 项通过**（5.55 秒）；隔离目录 `npm ci`、TypeScript 与 Vite build 通过，Chromium 全量 **63 项通过**（27.2 秒）。Python/Vue 行数约束、文档本地链接与 `git diff --check` 通过。新发现的 GET 掩盖提交失败、缓存卡顿使核验过期、日程短暂失败丢失保护标记均先以回归测试复现，再修复通过。

确认窗口内重启、确认记录在开始前丢失两项回归先复现失败，再通过登记标记和工作者启动时间保护修复。1280×800 V5 模拟会议截图确认面板完整、整页无溢出；截图是隔离测试数据，不是实际会议。

## 后续独立环境启用

1. 先核对部署 SHA、目标独立服务、Redis AOF 和 HTTPS，保留旧代码及配置回退。不得将本地构建直接覆盖当前样机或旧服务器。
2. 在私有配置中设 `ROOM_DISPLAY_USAGE_ENABLED=true`、`ROOM_DISPLAY_USAGE_WRITES_ENABLED=false`，按批准流程重启新环境。
3. 通过现有 CLI 在该环境签发独立操作凭证：`python -m app.services.room_display_admin issue-usage omm_目标ID`。凭证一次性显示，私下交付，不进入 URL、日志或仓库；撤销用 `revoke-usage`。不影响旧展示凭证。有效期 30 天，同房间重新签发会撤销旧操作凭证。
4. V5 门牌保留既有展示绑定，在「管理员配置」输入该房间操作凭证；中控选「V5 · 平板与服务器」，再设置观察模式。
5. 工作者须在会议开始前 5 分钟（默认确认窗口）之前启动，门牌在窗口内持续在线。中途重启或错过窗口的预约会暂停处理，等待下一场。门牌每 10 秒查询并主动上报健康状态，超过 45 秒无有效心跳则暂停未确认实例释放；普通 GET 查询不再续期。
6. 虚拟房间先用短预约完成观察和故障测试，通过后进入专用预约写入验证，不强制等待三个工作日。真实房间推广另行确定观察期。配置变更后当前旧策略实例不沿用自动操作；不要在已开始的会议上反复切换模式验证。
7. 真实释放测试仅针对明确允许取消的测试预约。先核对该房间原生签到规则，验证公开 API 的普通单次预约释放及通知效果，再允许勾选对应验证项。
8. 真正发送飞书写请求还需：开启 `ROOM_DISPLAY_USAGE_WRITES_ENABLED`、将该房间 ID 加入服务器 `ROOM_DISPLAY_USAGE_RELEASE_ROOM_IDS` 白名单、房间选自动、两个验证项均成立、当前实例勾选「非重复且允许释放」、解除全局暂停。单纯进入 V5 或签发凭证不会启用自动释放。

## 失败与回退

- `blocked`：当前实例自动操作已暂停，排查监控、凭证、策略或登记条件。不要直接改 Redis 强行重置为 pending。
- `uncertain`：可能已执行，先在飞书核对目标预约和忙闲；系统不会自动重发。没有对不明状态一键补发的入口。
- `failed`：飞书明确返回业务错误；例如 105003 需重新核对实例时间。现有版本保留失败记录，人工处理后等待新实例，不自动修改参数重试。
- 立即停用：中控「暂停所有房间释放」；随后停止 V5 工作者或关闭功能配置并按流程重启。已发出的请求不能被开关撤回。
- 回滚到旧展示代码时保留 `rooms:usage:v1:*` 记录，不清空 Redis；已释放的预约不能靠代码回滚恢复，不自动抢占重新预约。
- 策略、状态、解除暂停记录采用 7 天失效保护，灰度期间注意规则有效性；审核日志最多 1000 条，需长期保存时另外实施受控导出。

## API 与状态

所有响应 `Cache-Control: no-store`，沿用 `{code,message,data}`。旧读 API 不变。

| 方法和路径 | 凭证 | 用途 |
|---|---|---|
| GET /api/meeting-rooms/usage | V5 操作凭证 | 只读查询确认状态及 target_id，不刷新终端心跳 |
| POST /api/meeting-rooms/usage/heartbeat | V5 操作凭证 | 协议 2 主动健康上报及核验回复，校验页面会话、实例、策略和时效 |
| POST /api/meeting-rooms/usage/confirm | V5 操作凭证 | 确认当前实例，重复确认不重复写业务 |
| POST /api/meeting-rooms/usage/end | V5 操作凭证 | 提交提前结束命令，由后台核验执行 |
| GET /api/room-control/usage/{room_id} | 主控凭证 | 规则、当前实例、房间及全局审计 |
| PUT /api/room-control/usage/{room_id}/policy | 主控凭证 | 更新房间规则并生成新版本 |
| POST /api/room-control/usage/{room_id}/verify | 主控凭证 | 登记明确允许释放的非重复实例 |
| PUT /api/room-control/usage-pause | 主控凭证 | 设置全局暂停开关，默认暂停 |

确认与结束携带 occurrence_id、policy_revision、session_id；旧客户端缺少页面会话不能执行操作。心跳携带 protocol=2、session_id、occurrence_id（无目标时为空）、policy_revision、operation_state（ready/submitting/uncertain）、可选 challenge_id。凭证仍取自 Authorization，不放 URL。实例标识结合 room_id、uid、original_time、开始与结束时间，不把日程改期当作同一次确认。状态查询代替另建操作结果接口；释放成功后仍以刷新后的预约数据判断房间是否空闲。

## 本轮未验收

飞书真实释放及通知、员工创建会议的操作范围、重复日程影响范围、原生签到策略协调、Redis 主机故障恢复、Android V5 实机、试点三工作日观察均未完成。上述事项不阻塞本地默认关闭的代码交付，但阻止对真实房间启用自动释放。

## 单房间释放白名单

服务器配置 `ROOM_DISPLAY_USAGE_RELEASE_ROOM_IDS` 为 JSON 房间 ID 列表，默认空列表，禁止所有房间释放。未列入的房间即使被误设为 V5 自动模式，也不能自动释放或提前结束；任务认领前及调用飞书前均复核。该配置不在 Control 中开放修改。主控返回的 `writes_enabled` 现表示当前房间是否获服务器写入资格，不再仅表示全局环境开关。

开启专用房间时仍保留全局暂停、策略核验、平板操作凭证与每次非重复预约登记；不绕过健康核验或恢复补释放。


## Control 单次点击修订（2026-09-20）

19:31 只读核查：截至当时没有用户签到 POST 或成功审计；19:15–19:30 预约为 blocked/missed_window、无释放动作。Control GET 持续 200，说明状态读取正常，不能将点击入口解释为签到已入库。

白名单 V5 预览取消「进入签到测试」中间步骤，直接显示真正提交的「确认使用」。无当前预约时显示「当前没有可签到的预约」，不发送签到；最近一次成功时间来自服务器审计，不从本地点击推断。此变更只更新前端静态资源，后端 API、白名单、规则、凭证及平板保活条件不变。
