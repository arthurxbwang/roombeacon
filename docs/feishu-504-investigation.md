# 飞书忙闲接口 504 排查（2026-09-21）

## 结论与边界

已定位到 Control 后端对 `https://open.feishu.cn/open-apis/meeting_room/freebusy/batch_get` 的出站 GET 收到 HTTP 504。失败约发生在请求开始后 6–7 秒，早于客户端 30 秒超时；不是本机 Nginx 等待门牌请求产生的 504，也不是签到或释放写入接口失败。

现有证据支持上游忙闲查询链路间歇超时；尚不能区分飞书入口网关、内部依赖或其他上游中间层。历史日志未保存失败响应的追踪号及响应头，不能声称已经确定飞书内部根因，也不能用当前资源读数排除故障发生时的瞬时负载。

## 历史日志核对

读取现有 `room-display` journal，范围从 2026-09-20 00:00 UTC 至本次约 2026-09-21 12:18 北京时间。下表全部为北京时间，耗时由秒精度的请求开始与失败日志近似关联，并非已有逐请求精确指标。

| 时间 | 近似耗时 | 结果 |
| --- | --- | --- |
| 10:54:20 | 7 秒 | 批次 14 失败；该轮 323 间刷新、1 批失败 |
| 10:59:37 | 6 秒 | 批次 16 失败 |
| 11:00:17 | 6 秒 | 批次 6 失败；上述两批所在轮次 303 间刷新、2 批失败 |
| 11:06:07 | 6 秒 | 首次尝试 504，重试后该轮 343 间全部刷新 |
| 11:31:21 | 6 秒 | 首次尝试 504 |
| 11:31:29 | 7 秒 | 同一读取第二次尝试仍 504，第三次恢复；该轮 343 间全部刷新 |

日志可确认至少 6 次 504，涉及 5 次逻辑读取。同期记录 6,162 次忙闲请求开始，粗略比例约 0.10%；这不是完整请求成功率，日志包含不同版本、重启及重试，不能用它替代长期指标。先前报告“期间两次”仅指 11:15–12:00 验收中的两次响应，并非全天累计。

截至本次读取，最近三个完整采集周期分别于 12:07:03、12:12:05、12:17:08 完成，均 343 间刷新、0 批失败。

## 请求方式与实测

- 当前后端 `374db89fe3afb6cd7da185cb260925e7a58b8afa`，静态 `da6315263fde70f4351a26f64d40a9edb17823a6`。
- 中央采集默认每 300 秒一轮，343 间按每批最多 20 间分为 18 批，逐批执行；基础忙闲流量约每分钟 3.6 次，另有重试和 V5 核验读取。平板与 Control 轮询只读缓存。
- [飞书官方 Pad 整合方案](https://www.feishu.cn/content/7271149634339618818)也建议每 5 分钟拉取忙闲数据；这只是采集周期参考，不等于证实应用未受共享配额影响。尚无证据把本次 504 归因为限流。
- 查询窗口为上海昨天零点至后天零点，共三天。未证明批量大小或时间窗口导致此次超时，不据此盲目缩短窗口或提高请求频率。
- 服务进程无 HTTP/HTTPS/ALL_PROXY 环境变量，代码直接请求飞书 HTTPS，TLS 校验保持开启。
- 12:18 从同一服务器对目录排序中的原失败批次 14、16、6，各进行一次三天窗口、20 间的只读查询，相邻探测间隔 2 秒。三次均 HTTP 200、业务 code 0、错误房间数 0，耗时分别 4.295、3.956、2.352 秒。
- 当前成功响应有 `Server: Tengine`、`Via` 和飞书 `x-tt-logid`／`x-request-id`。这些是成功探测的头，不能冒充历史失败响应的证据。没有保存响应体、凭证、日程内容或人员信息。

## 服务器资源

当前内存可用约 3.8 GB／4 GB，磁盘使用率 7%，无交换使用；uvicorn 进程累计 CPU 占比约 1.3%。load average 约 18，不能单独判定应用过载：同时读取的 CPU 压力近 10／60／300 秒均 0，cgroup 没有 CPU 限流记录，内存压力为 0，IO 压力低。当前资源检查未发现服务耗尽，但没有故障时刻的历史资源采样，保留这一限制。

## 当前处理与后续定位

已有补丁仅对忙闲 GET 的 502／503／504 做最多三次尝试、1 秒／2 秒退避，真实验收已验证恢复。持续失败仍让缓存按原有效期过期并进入保护；释放写入不因本补丁重试。10:54 的旧版本确实曾因此中断监控，新版本本轮成功恢复不代表以后所有上游故障都能恢复。

要进一步追查飞书内部原因，需要在后续诊断迭代中记录失败请求的精确耗时、状态、批量数量和经过字段白名单／长度限制的请求追踪号，再结合故障时刻资源指标向飞书核查。该诊断能力尚未实现，本轮只读排查并保存结论，没有新增部署、修改房间规则或执行会议释放。


## 17:31再次影响测试房

17:30:50/57重试后17:31:05第14批仍失败，该批含IT灯塔-Test。缓存过期，平板与服务器仍连通但V5返回409；17:35:36采集恢复、17:35:59签到成功。本场不算B01释放通过。详见[B01故障排查](v5-b01-connection-investigation.md)。本次无新部署，504未根治。
