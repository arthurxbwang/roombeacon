# BX68 接入正式服务器（2026-09-22）

> 后续更新：仅IT灯塔-Test已启用生产V5签到及自动释放，新界面43d8b8f已上线；下文开关关闭为接入时状态。见[当前签到记录](production-v5-checkin-2026-09-22.md)。

目标为既有13.3寸BX68样机 `10.0.68.146:5555`，用户指定正式服务器 `roombeacon.thundersoft.com`，保留IT灯塔-Test与V5页面选择。APK仍为0.2.4-debug，本次没有重装应用或部署服务器源码。

**当前已完成：用户更换Wi-Fi后，设备地址为 `10.0.51.221:5555`，已直接通过HTTPS连接正式域名并绑定IT灯塔-Test；没有ADB反向转发，应用重启后绑定及V5选择保留。下方旧网段阻塞为历史排查记录。**

## 正式服务器核对

HTTPS从开发机正常校验证书并返回200；SSH8081登录主机 `tsm-eed-ts-bj`，发布目录为 `/data/roombeacon/releases/3b0bf39cdfdb92e421882444615f8ab7bcb76c8c`。HTML发布标识为 `188562c6-7cf7-4836-9ecb-e8c60e41e6e5`。

正式环境已包含IT灯塔-Test，ID为 `omm_d42ad8a9e50c5ddf9d60fe3d3bc6473b`。签发前确认该房间没有展示或操作凭证，首次签发生产展示凭证并在服务器及本地忽略目录私密保存，权限600。旧测试环境凭证保持不变，没有迁移旧Redis或签到状态。开发机使用新凭证读取真实展示接口200，房间匹配且缓存有效；这不代表平板已绑定成功。

生产环境V5功能和飞书写入开关均关闭，释放白名单为空、测试房间无V5规则，展示owner为official。本次未开启生产签到、自动释放或复制旧环境规则。

## 旧网段排查记录

当时已通过维护密码界面把origin保存为 `https://roombeacon.thundersoft.com`，原灯控开关和维护设置保留。更换origin按既有流程清除旧网页绑定，但新生产绑定曾因网络不通而暂停。

- 实际接口为wlan0、地址10.0.68.146、网关10.0.68.1；eth0为down。
- 平板系统无法解析正式域名；直接向其DNS223.5.5.5发起查询返回NXDOMAIN（rcode3）。开发机查询同一DNS及自身DNS均返回10.0.53.174，存在不同网络下的解析差异。
- 从平板直接连接10.0.53.174:443也超时，因此仅更改域名解析不能证明连通性恢复。
- 当时设备停留在Wi-Fi网络详情页，未打断用户网络调整；用户随后确认原网段不能访问新服务器，并提供更换Wi-Fi后的新IP。

曾准备仅监听开发机127.0.0.1:8769的临时展示转发，固定连接正式HTTPS域名并正常校验证书，仅允许目标房间展示接口；没有把应用切到该入口。换网后设备反向转发表为空，已停止未使用的本地转发进程，当前生产接入不依赖旧8768或临时8769通道。

## 新Wi-Fi直连实机验证

- 新地址10.0.51.221/24，仍为同一RK3568 BX68固件；wlan0正常、eth0为down。本次验证Wi-Fi直连，不宣称有线以太网通过。
- 平板可将正式域名解析到10.0.53.174，系统WebView正常加载HTTPS，未关闭TLS校验或安装额外证书。
- 实际网页绑定新生产展示凭证，房间ID及名称匹配；首次请求和后续轮询均HTTP200，日程缓存有效，V5选择持久保存。
- 在 `adb reverse --list` 为空时，强制停止并重新打开APK，仍自动恢复正式域名、生产房间绑定和V5选择；再次真实请求200且数据有效。验证的是应用重启，未执行整机断电或开机自启动测试。
- 实际视口1280×720、DPR1.5、整页无溢出；状态busy，IO1/2/3=101、IO4=0，符合BX68红灯接线配置。
- APK仍为0.2.4-debug，本次仅切换接入与绑定；生产V5功能和写入继续关闭，owner=official。保存V5页面选择不等于开启服务器签到或自动释放。

## 后续与回退

正式域名直连与应用重启恢复已通过；有线接入、整机重启自启动、断网恢复及正式签名APK发布仍需另做验收。生产签到和自动释放需明确启用范围，不自动复制旧服务器的测试规则。

回退仅需在维护页恢复此前8768测试入口，并用已有旧环境凭证重新绑定IT灯塔-Test / V5；不清空应用数据、不轮换旧凭证、不改变生产服务器开关。真实凭证不进入本记录或Git。
