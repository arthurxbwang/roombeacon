# V5 重复实例释放实现与验证（2026-09-21）

## 本轮范围

用户已创建IT灯塔-Test三组每日重复系列，要求继续真实签到与释放验收。当前只对此房间开放V5写入，其余342间官方方案不变。

[飞书官方Pad方案](https://www.feishu.cn/content/7271149634339618818)明确区分：重复日程original_time为0作用于整个系列，非0用于单个实例。忙闲接口普通重复实例返回0，例外返回原始时间；不能直接把普通重复实例的0用于释放。

本次增加管理员按实例登记`recurring_verified=true`，与原有`non_recurring_verified=true`互斥。当前试点仍逐实例登记，不自动批准整个系列或未来预约。只有原始时间为正数的例外，或新鲜快照中存在同UID不同开始时间的重复实例，才可登记；普通实例取开始时刻的Unix秒数，改期例外保留上游原始时间。忙闲快照身份、签到身份保持兼容。

发送前再次读取飞书，重新核对身份、重复依据及登记时的正数原始时间。旧非重复资格遇到已知重复系列会拒绝写入。释放仍只尝试一次，没有退回0重试的路径。释放后检查相同查询窗口内原有后续实例仍存在，否则标记不确定并使旧快照过期，不冒充成功。

## 检查

后端210项检查及Ruff通过。新增回归覆盖：重复误登记为非重复、无证据的单实例、管理员认证、混合登记拒绝、实例正数定位、例外改期、后续丢失、目标仍占用、依据丢失、定位变化、旧资格拒绝及同系列签到隔离。上游由mock隔离，Redis为临时独立进程。

## 真实测试进展

- 今天14:15–14:30第一场：14:21:11平板实际签到成功；明天同系列仍无签到记录。
- 第二场14:30–14:45：部署重启后进入blocked/monitoring_interrupted，未登记、未发送释放；保留原记录，没有清除或重置。
- 第三场14:45–15:00：14:35:10观察到pending/monitoring，真实平板同时覆盖两场，缓存14:34:05已更新；14:45:03通过认证verify接口登记成功，release_scope=recurring_instance，release_original_time=1789973100（今天14:45的Unix秒）；14:45:08平板心跳切到第三场。14:55:00进入waiting，14:56:00进入平板核验，14:56:08唯一claim，14:56:11.255得到released/verified_release。

本轮已验证每日重复普通实例的单次释放及后续日期保留。实际仅第三场执行自动释放；第二场是部署重启保护测试，不能描述成两场自动释放通过。改期例外仅有隔离回归，尚未真实验收；周/月重复、长稳及自动登记整个系列不属于本轮完成范围。

## 已部署后端

- SHA：`7deb6ba97818d30e11677c1cb7f5ae8e06c3f5c0`，GitHub codex/v5-control-release发布，Control仍为10.0.24.208。
- 后端210项回归、Ruff和diff检查通过；无前端改动，静态仍为da63152。
- 部署前短暂停写，保留42条终态记录、未核验第二场记录、原规则及凭证。其余342间官方/off，只有测试房写入白名单，全局暂停已解除。
- 私有回退备份：`/var/backups/roombeacon-control/20260921-7deb6ba-recurring`。回退先暂停写入并核对无在途请求，恢复备份50-roombeacon-v5.conf，daemon-reload并重启room-display，回到374db89；保留Redis记录，新版重复登记记录不应在旧版解除暂停使用，须先保持暂停并完成核对。
- 仅对锁定的今天14:45第三场运行有截止时间的监测／重复实例登记。监控脚本不生成心跳、不重置blocked、不直接调用飞书释放，15:01前退出。

## 最终独立核验（14:57）

14:57:08从现有服务器独立直查飞书三日窗口，与14:30:37基线逐项比较：9个实例变为8个，只移除9月21日14:45–15:00第三场；三组系列在9月22日、23日的6个实例全部保留，没有新增或其他缺失，今日前两场也保留。

审计确认第三场只有1次claim与1次成功result；接口code成功后后台读回验证，未走任何0定位回退或重复写入。释放后的真实缓存采集时间14:56:10.217、有效至15:02:10，平板心跳ready；14:57实机显示空闲可用，右侧仍显示明日预约。

343间规则API全部200，其余342间official/off、无V5执行记录；201/203/205官方二维码状态与私有配置一致，设备凭证指纹保持。临时监测脚本14:56:13完成后已退出，不继续登记未来实例。原房间白名单和规则未扩大。

期间14:39:18另遇一次忙闲504，重试恢复，该轮14:40:06完成343间、0失败，第三场未中断。此前14:02重试耗尽仍是需要跟进的上游稳定性问题，不能以本次成功宣称504已消除。

本地忽略目录`.local-tools/v5/`保留recurring-monitor.jsonl、recurring-before-release.json、recurring-final-upstream.json、recurring-final-evidence.json、recurring-final-scope.jsonl和实机截图。日程实际UID、凭证及上游响应体未提交。
