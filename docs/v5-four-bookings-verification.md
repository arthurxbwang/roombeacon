# 2026-09-21 四场非重复预约实测

用户确认全部为不重复的专用测试预约，授权充分测试与修复。唯一写入房间为IT灯塔-Test；其他342间仍为官方方案。测试使用真实BX68平板心跳，实例核验通过现有认证Control API；没有模拟心跳、重置保护记录或直接调用释放接口。

## 最终结果

| 预约 | 实际结果 |
|---|---|
| 09:15–09:30 | 09:07缓存异常后租约重建晚于09:05窗口，受保护；09:17:10真实ADB点击确认，服务器confirm/confirmed |
| 09:30–09:45 | 09:20:04提前登记，09:30:01完成非重复核验，09:41:07自动释放；09:41:45独立飞书查询确认移除，后续两场保留 |
| 09:45–10:00 | 09:35:04提前监控；旧版本释放后缓存过期导致09:42:18受保护，09:45:32真实平板签到成功 |
| 10:00–10:15 | 09:50:06提前监控；10:01:01签到成功；10:02:18平板二次确认提前结束，10:02:28释放成功 |

首次真实签到截图保存在本地调试目录 `.local-tools/v5/confirmed-first.png`（不入Git）。09:30这一场已完成真实未签到释放，并独立读取飞书忙闲复核；参与人通知未验证。后续预约保留的证据限于这些独立非重复测试场次，不代表重复日程已验收。

## 运行版本与范围

09:16后端从GitHub准确SHA `718eebb68f3a433a91769c45791ec56846a52aac` 更新，静态仍为 `85a3cf2e9afbdc0183e2f9c30ab1156011aa6a09`。171项后端检查及Ruff通过，前次前端70项通过，此次未改前端。

切换前无活动监控实例；32条已有记录、设备凭证、房间策略和单房间白名单保留，343间逐一核对通过。备份与回退见[夜间修复](v5-overnight-verification.md)。

09:05开始的采集在09:07出现批次TimeoutError及Redis连接ExternalAPIError，09:07:28工作者租约重新建立；测试房间缓存09:10:39恢复。当前Redis仍运行原进程，未重启或修改Redis配置。具体主机延迟根因尚未证实，不把累计磁盘同步延迟计数当作本次原因的证明。

## 释放后缓存补丁（隔离验证）

实测等待期间，通过真实缓存读取路径的隔离回归复现：旧逻辑在释放成功后将整房间缓存立即过期，后续实例无法继续读取新鲜数据，可能再次受保护。补丁改为用释放后飞书完整忙闲查询的结果更新缓存；查询窗口与中央采集一致，后续新建预约也从新响应获取。并发采集只允许不早于现有快照的结果原子写入，避免旧的慢查询把已释放预约写回。

读回失败、房间缺失、非法数据或仍占用时保留不确定结果、不重试写请求；过期旧缓存且不制造空闲。忙闲快照不复制旧主题，主题由后续正常采集补齐。182项后端检查及Ruff通过，其中连续后场、上游新增预约、读回错误、并发覆盖和损坏缓存恢复均有隔离测试。09:43已部署后端 `c6658591d59052fe9673f5bd37d4163d2e9a1056`，静态仍为85a3cf2。切换时无活动监控/释放记录，34条记录保留，其他342间仍为官方方案。备份 `/var/backups/roombeacon-control/20260921-c665859-readback/`；回退先暂停V5并核对在途请求，恢复该目录备份的服务drop-in后重启，回到718eebb，不清Redis。第四场已在新补丁上真实提前结束并及时刷新，详见下方记录。

## 已完成的真实自动释放链路

- 09:40:01.110：第二场进入等待，仍可补签到。
- 09:41:01.558：发出平板核验挑战。
- 09:41:05.590：真实平板确认后认领唯一释放操作。
- 09:41:07.965：飞书写入成功，随后忙闲读回不再占用，记录released/verified_release。
- 09:41:45：独立只读查询再次确认仅09:30–09:45消失，第一场历史记录与09:45、10:00预约仍在。

当时后端仍是718eebb，因此也观察到旧缓存问题：快照valid_until被改为09:41:06，平板停止保活；09:42:11下一轮采集恢复，第三场于09:42:18因连续性中断受保护。没有将第三场保护清零；上线c665859后仅通过真实按钮完成签到保留。

## 第四场：新版本提前结束及缓存刷新

- 09:50:06.902：开始前建立独立监控；第三场此前的保护不影响本场。
- 10:00:00.546：依据用户明确确认，经Control认证API登记该非重复实例。
- 10:01:01.026：BX68真实点击确认使用，服务器保存confirmed。提交过程先短暂保护本次操作，再由成功确认转为confirmed，这是既有提交保护流程。
- 10:02:18.199：真实平板点击「提前结束」，核对房间与10:00–10:15时段后点击二次确认，记录end_requested。
- 10:02:26.032：平板核验通过后认领唯一操作；10:02:28.946返回released/verified_release，release_kind=end。
- 10:02:27.801：新补丁的完整飞书忙闲读回已发布为新快照，有效期至10:08:27；没有把房间缓存强制过期。10:02:43实际心跳ready、距最近约8秒，已无待监控预约。
- 10:02:47：独立飞书查询只保留第一/第三场历史预约，第二/第四场均已移除。10:03真实ADB截图显示「空闲可用」，无过期错误；截图 `.local-tools/v5/fourth-released-live.png` 不入Git。

四场有界监测脚本于10:02:30在全部终态后自动退出，没有留下针对未知新预约的自动登记脚本。两次真实释放分别为NOT_CHECK_IN和ENDED_BEFORE_DUE，每场各一次；没有跨房间释放或重试模糊写入。

## 最终范围核查与边界

10:03:31逐一检查343间房的认证API，无失败；仅IT灯塔-Test为V5自动且可写，其余342间为official/off、无V5写入和提前结束资格。其他房间无执行记录；201/203/205官方展示接口200，二维码配置存在状态一致；所有设备凭证与部署前指纹一致。10:03:24–10:04:52新版本完整采集18批、343间，失败批次0，后续正常采集未出现异常。

- 代码与隔离验证：182项后端测试及Ruff通过；前端未改，沿用前次70项浏览器检查。本轮使用真实APK、真实服务器、飞书读写与独立读回完成实测，没有用mock冒充线上。
- 已证实：非重复预约签到持久化、前场保留时后场提前监控、一次未签到自动释放、一次提前结束、新版释放后缓存与页面恢复、其他房间隔离。
- 连续释放后未来实例保持监控的新版边界由隔离回归覆盖；本轮第三场在旧版本中已受保护，不能说新版完整连续释放链路已全部实机通过。
- 仍属试点：重复日程单次实例影响、参与人通知、跨房间同一日程、多日长稳尚未验收。零点修复经跨日回归通过，本轮没有等到下一个真实零点。
- 新增预约仍需在Control→V5规则勾选「我已核对这是一场允许释放的非重复预约」，点击「仅登记当前实例」。本轮只对用户确认的需释放实例登记；房间开关不等于自动批准未知未来预约。
