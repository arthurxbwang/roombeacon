# V5 连续预约提前监控修复

日期：2026-09-20。目标：仅在既有 Control 服务器及 IT灯塔-Test 测试房间修复连续预约漏监控；其他房间维持官方方案。

## 原因和行为

旧版仅监控当前展示目标，前场保留至结束会让后场错过开始前登记。新版将展示目标与提前监控集合分离；平板显式确认本房间集合，服务器为每次预约分别保活。前场已签到、未签到、受保护均不妨碍后场进入监控窗口。当前确认提交不确认或中断下一场；异常、断线、会话变化、过期、重启继续保护相应预约。

不改变非重复预约登记要求、房间写入白名单、原生策略核对、释放等待时间或发出前核验。重复日程释放仍待单独开发验证。

## 隔离验证

- 新回归先在旧代码复现缺少后场监控；修复后覆盖前场三种状态、切场前后持续保活、前场正常确认、离线/异常/会话变更/重启/漏报、跨房间/删除/改期/重叠/窗口外拒绝、旧页面兼容。
- 模拟完整流程：前场已确认且保留，后场开始前登记、管理员登记非重复，超时等待60秒、平板挑战核验、飞书前置与结果查询，只向后场发送一次 NOT_CHECK_IN，之后的预约保留。
- 慢外部核验跨越下一场开始时间时，不得用旧时间登记为已提前监控。
- 前端：npm ci、类型检查/构建、70项Chromium测试通过，包含心跳显式覆盖前后两场而确认仅提交当前场。
- Python：168项测试、Ruff通过；所有飞书写入测试均为mock，不能作为真实释放验收。

## 部署与实机

已部署至既有 Control `10.0.24.208`，前后端运行 SHA 均为 `85a3cf2e9afbdc0183e2f9c30ab1156011aa6a09`，源码由服务器从 GitHub 获取；未复制开发机源码。

部署前核对无正在执行的测试实例，暂停V5后切换，校验全部343间：仅IT灯塔-Test可写，其他342间official/off；原8条终态记录、房间规则和凭证按切换前基线一致。静态文件内容校验通过后解除暂停。

BX68实机通过现有ADB通道刷新到该SHA，真实心跳200，已携带显式多场监控数组。检查时尚无预约进入提前窗口，数组为空；此事实不能冒充两场真实并行监控验收。实机仍为1280×720视口，三列标题底边差低于0.001px，面板底边一致，无整页溢出。

真实新的两场预约释放验收待用户确认准确时间及非重复条件后进行；本次不自动登记明天凌晨缓存中的其他预约、不代点签到、不恢复已有blocked记录。

备份：`/var/backups/roombeacon-control/20260920-85a3cf2-adjacent/`。回退先暂停V5并核对无在途请求，恢复备份中的`50-roombeacon-v5.conf`与`room-display` Nginx站点配置，执行nginx配置检查、systemd daemon-reload/restart及nginx reload。恢复后端`781b409841a571fb54ae0c720b234f880537bc32`、静态`f108b8575e12b54611c1ae84bb4e01713a9c4b1e`；保留Redis记录并维持暂停，核查后再决定继续测试。

部署曾被自动审批以共享服务影响范围拦截。核查确认目标为用户此前明确要求更新的独立Control，并提交已有授权及“无预约释放API、无房间规则修改、只读核查343间”的脚本证据后，同一部署经自动审批放行执行，未绕过审批。
