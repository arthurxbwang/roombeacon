# 生产 IT灯塔-Test 签到与自动释放（2026-09-22）

## 启用范围

用户明确仅对 IT灯塔-Test 启用签到与自动释放，并选择「先沿用现有预约，按已核验实例测试」。正式环境为 `https://roombeacon.thundersoft.com`，SSH8081；不修改旧服务器。

- 唯一房间：`omm_d42ad8a9e50c5ddf9d60fe3d3bc6473b`；owner=v5、mode=auto。
- 会前10分钟开放签到，开始后10分钟截止，再等待60秒进入释放核验；未到场也仅能释放已核验的具体实例。
- 全局 V5 功能与写入开关启用，释放白名单只含该房间；自动核验日历映射仍为空。未把新预约或重复系列默认认定为安全可释放。
- 逐一通过鉴权API核对343间：目标可写，其余342间规则与启用前完全一致且不可写；所有展示凭证摘要保持不变。
- 为目标房间签发生产操作凭证并绑定 BX68 样机；实际心跳200、房间匹配。真实凭证仅存私有备份及设备，不入Git。

功能开关由 `/data/roombeacon/shared/config/v5-it-test.env`（600）和 `/etc/systemd/system/roombeacon.service.d/60-v5-it-test.conf` 生效。后端仍为 `3b0bf39cdfdb92e421882444615f8ab7bcb76c8c`；开关生效时重启一次服务，未迁移旧环境Redis或受保护记录。

## 界面

签到区取消独立背景和边框，使用带对勾的大号渐变胶囊按钮。已绑定设备隐藏设置入口；未配置设备使用简短「签到暂不可用」及「设备设置」。

按服务器截止时间显示逐秒签到倒计时；处于待释放、已核验且未暂停时显示琥珀色释放倒计时。到零仅等待服务器确认，客户端不会自行宣称预约已释放。失联、过期、暂停、未核验或观察模式不显示有效释放倒计时；原有签到凭证、健康监控、保护和提前结束关闭逻辑保持。

1280×720布局截图发现待释放阶段重复说明挤压按钮，已先增加失败回归再精简重复时间说明；无需滚动即可完整显示按钮。

## 检查与发布

- 从生产准确基线新建 `codex/production-v5-checkin-20260922`，只包含签到组件、对应回归和文档，避免混入开发工作区其他改动。
- `npm ci`、`npm run build`通过；完整 Chromium 79项通过，视觉复核后补充内部溢出及到零保护回归，最终定向9项检查全部通过。
- 截图为明确标注的模拟预约，用于确认布局与倒计时；不作为线上签到或释放证据。
- 当前生产没有进入签到窗口的实例，本轮未触发真实签到或飞书释放，也没有为未来未核验实例补造资格。
- 静态发布从GitHub拉取准确SHA并在生产构建；Nginx仅切换静态根目录，保留上一版本哈希资源。后端源码及当前链接保持3b0bf39，发布完成后补充实机回执。

## 备份与回退

启用前快照位于 `/data/roombeacon/backups/20260922-v5-it-test/`（目录700），包含全部房间策略、展示凭证摘要、旧环境配置及新操作凭证（文件600）。不要输出或提交这些私有文件。

UI回退：恢复该目录中的 `nginx.before-ui.conf` 到原Nginx配置目标，执行 `nginx -t` 后 reload，静态根目录回到 `/data/roombeacon/current/frontend/dist`。无需替换后端或清空设备绑定。

若需要撤销本次业务启用，先通过现有主控暂停释放、核对在途状态，再将单房间环境覆盖的写入开关设为false并重启。按 `before.json` 恢复目标原规则，不回写其他342间，不删除审计或强制重置受保护记录；新凭证如需撤销，使用既有撤销流程。
