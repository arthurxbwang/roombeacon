# V5 BX68 三列对齐与释放状态核对（2026-09-20）

用户授权通过 ADB 截图检查 IT灯塔-Test 平板画面，并继续专用房间自动释放测试。本轮只修改 H5 显示，未点击确认、提前结束或修改预约。

## 实际检查

- 设备 `10.0.68.146:5555`，BX68，物理截图 1920×1080；WebView 1280×720、DPR 1.5。ADB 截图与只读 WebView 几何数据均来自实际页面。
- 原状态文字下沿 254.16，中间状态下沿 270.90，右侧标题区域下沿 225.94 CSS px；左侧进度底边 532.35，中间卡片 544.98，右侧内容 558.35。中间还存在 32 px 内部溢出。
- 20:03:53 服务器确认协议 2 平板心跳为 ready，最近上报 20:03:50，距检查 3.7 秒；展示和操作凭证已存在。当前 19:45–20:15 预约仍为 blocked/missed_window，未登记允许释放。
- 晚绑定不会使受保护旧预约恢复释放。下一场完整测试须使用新的非重复预约，提前让采集与平板覆盖签到窗口，并在 Control 登记该实例允许释放；不能修改 Redis 强行重置旧实例。其他房间仍为官方方案，白名单仅专用测试房间。

## 页面变更

- V5 横屏使用同一行布局三个标题、同一行布局三个内容区域，标题下沿和内容底边各自对齐；增加专用主内容分组，V4 默认仍用 display:contents 保留原布局。
- 中间固定标题「签到与释放」，释放状态独立呈现；简化卡片边框、圆角及按钮，正常确认动作仍明确可点击。
- 受保护时仅提示不会自动释放，已确认时提示本次免于未签到释放，待登记时说明缺少实例登记；不再同时展示相互矛盾的释放文案。过期预约隐藏无效确认按钮。
- 保留单房间凭证、主动心跳和一次核验机制；不代替平板上报，不改变任何释放规则。

## 验证

- 对齐与受保护文案回归先复现失败，再修复；1280×720、1280×800 的标题和内容底边差均要求不超过 1 CSS px，页面与状态面板无溢出。
- 完整浏览器检查中 68 项通过，原页面发布测试出现 response disposed；单独重跑复现后改为在轮询前读取固定 HTML 响应，并在退出前等待路由完成；两个页面发布用例均通过，合计 69 项覆盖通过。TypeScript/Vite 构建与 diff 检查通过。
- 模拟页面验证与实际 ADB 截图分开保存，未把模拟预约作为真实自动释放结果。


## 部署与实机结果

- 静态页面发布 SHA `f108b8575e12b54611c1ae84bb4e01713a9c4b1e`，从 GitHub 构建；后端仍为 f3a07ea，PID 和工作目录不变，未重启后台或修改房间配置。
- 服务器实际 HTML SHA 及脚本／样式哈希与发布目录一致。Nginx 回退备份 `/var/backups/roombeacon-control/20260920-f108b85-v5-layout/nginx.conf`，恢复并检查、reload 即可回退静态页面。
- 平板刷新后真实协议 2 heartbeat HTTP200，房间为 IT灯塔-Test；当前旧实例仍 blocked/missed_window，未被代理点击确认或释放。
- 实机三个标题下沿均为 244.375 CSS px，最大差 0.0000153 px；三个内容区域底边均为 558.354 CSS px，差 0；页面及三个内容区均无内部溢出。
- 实际 ADB 截图保存于忽略目录 `.local-tools/v5/layout/before.png`、`after.png`，几何结果为 `after-verification.json`。截图时间约20:11，预约19:45–20:15。未向实际平板注入虚构会议或健康状态。
- 下一场测试须让新预约在开始前被采集并由平板持续保活，Control 逐实例核对非重复并登记允许释放。当前旧预约不会补释放，真实飞书释放仍未验收。
