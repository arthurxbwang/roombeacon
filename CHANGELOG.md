# Changelog

## [Unreleased]

### Control 单次点击与签到回执
- 修正「进入签到测试」容易被误认作已提交的问题：白名单房间直接显示「确认使用」，点击才提交；无预约时明确提示当前不可签到，从服务器审计显示最近一次成功时间，会议结束后仍可核对。默认轮询不提交签到或保活，其他房间仍只读。

### Control 签到测试修复
- 专用 V5 房间预览新增显式签到测试入口，主控凭证通过服务器白名单、预约和窗口校验后记录确认；保持默认预览只读，不授予保活或释放能力。

### IT灯塔-Test 单房间释放（2026-09-20）
- 按用户授权部署 fa09d72，只为该房间开启服务器写入并解除暂停；新增默认拒绝的精确房间白名单，其他 342 间真实核验均为官方方案、无 V5 写入资格。保留规则和凭证，136 项后端检查通过，真实释放尚未验收。

### Added
- 为专用房间开放释放增加服务器白名单，默认空名单禁止全部写入；逐房间返回写入资格，名单外自动模式和提前结束均不可执行，并在发送前再次核验。
- 按用户授权部署 Control V4/V5（代码 4ba9d38）、保护协议 2 后端和对应 Nginx 路由；测试房间使用观察模式，飞书写入关闭。343 间房首轮采集通过，凭证与官方二维码保留，旧目录和配置可回退。
- 从 Argus 门牌基线 c34a561 抽取 Web V1/V2/V3、中控、独立采集服务、CLI、测试、部署模板及公司品牌素材。
- 补充交接、设计、架构、开发/运维手册、来源清单和 Android/ESP 对照规划。

### Changed
- 后续维护转到 RoomBeacon。独立配置不要求 Argus 数据库或 JWT 密钥；前端去掉平台登录/刷新依赖；飞书客户端只保留租户令牌及只读查询传输。
- 精简前后端依赖；保留 API、Redis 和浏览器存储键兼容。旧平台管理页面/事件入口归档而不纳入运行。
- 新增 RoomBeacon 路径模板，同时保留旧服务兼容模板。未实施服务器切换。

### Security
- 抽取的飞书传输错误不再记录原始 Token 响应或上游消息正文。

原生 Android/ESP：仅规划，尚无实现或固件。尚未发布 0.1.0。
